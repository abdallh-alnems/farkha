

import 'package:flutter/material.dart';

class FrakhType extends StatelessWidget {
  const FrakhType({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(body: ,))
  }
}