import 'package:flutter/material.dart';

class ArrowNon extends StatelessWidget {
  const ArrowNon({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.horizontal_rule,
      color: Colors.blue,
    );
  }
}
