import 'package:get/get.dart';
import 'package:intl/intl.dart';

class GetDateTime extends GetxController {
  RxString time1 = DateFormat('MM/dd').format(DateTime.now()).obs;
  RxString time2 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 1)))
      .obs;
  RxString time3 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 2)))
      .obs;
  RxString time4 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 3)))
      .obs;
  RxString time5 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 4)))
      .obs;
  RxString time6 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 5)))
      .obs;
  RxString time7 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 6)))
      .obs;
  RxString time8 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 7)))
      .obs;
  RxString time9 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 8)))
      .obs;
  RxString time10 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 9)))
      .obs;
  RxString time11 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 10)))
      .obs;
  RxString time12 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 11)))
      .obs;
  RxString time13 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 12)))
      .obs;
  RxString time14 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 13)))
      .obs;
  RxString time15 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 14)))
      .obs;
  RxString time16 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 15)))
      .obs;
  RxString time17 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 16)))
      .obs;
  RxString time18 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 17)))
      .obs;
  RxString time19 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 18)))
      .obs;
  RxString time20 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 19)))
      .obs;
  RxString time21 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 20)))
      .obs;
  RxString time22 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 21)))
      .obs;
  RxString time23 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 22)))
      .obs;
  RxString time24 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 23)))
      .obs;
  RxString time25 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 24)))
      .obs;
  RxString time26 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 25)))
      .obs;
  RxString time27 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 26)))
      .obs;
  RxString time28 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 27)))
      .obs; RxString time29 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 28)))
      .obs; RxString time30 = DateFormat('MM/dd')
      .format(DateTime.now().subtract(const Duration(days: 29)))
      .obs;

}
