<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme for Android 15+ (API 35+) -->
    <!-- Edge-to-edge is enforced automatically for apps targeting SDK 35+ -->
    <!-- Do NOT use deprecated attributes: statusBarColor, navigationBarColor, navigationBarDividerColor -->
    <!-- windowLayoutInDisplayCutoutMode is also deprecated in API 35 -->
    
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">false</item>
    </style>
    
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">false</item>
    </style>
</resources>
